import 'package:flutter/material.dart';

class ChaingLocationScreen extends StatelessWidget {
  const ChaingLocationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      
    );
  }
}